var React = require('react');

var AddScoreButton = React.createClass({
    render: function() {
        return (
            <div className="add-button">+</div>
        )
    }
});

module.exports = AddScoreButton;
